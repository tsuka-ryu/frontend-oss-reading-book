# 2026-09-23（水）

> 今日のひとこと：Next.jsの画像生成`next/og`に深刻度Critical（CVSS 9.5）のRCE脆弱性が見つかり、緊急パッチ（v16.3.6 / v15.5.26）が出ました。ほかにoxcがレキサーの曖昧トークン判定を状態機械に書き換え、長年のバグと性能問題を解消しています。

## 今日のリリース

- 【Next.js】v16.3.6：next/ogのImageResponse（Node実装）にあったCritical（CVSS9.5）のRCE脆弱性（GHSA-vcvr-r3jv-pc5j）を修正する緊急セキュリティリリース（出典: https://github.com/vercel/next.js/releases/tag/v16.3.6）
- 【Next.js】v15.5.26：同じ脆弱性の15.x系への修正バックポート（出典: https://github.com/vercel/next.js/releases/tag/v15.5.26）
- 【Rolldown】v1.2.10：watchの`addWatchFile`がディレクトリ指定に対応、CJS外部依存は`module.exports`をnamespaceスプレッドより優先するように、など複数の改善（出典: https://github.com/rolldown/rolldown/releases/tag/v1.2.10）

## トップニュース

next/og（`ImageResponse`）に、生成画像のクエリパラメータなどを細工するとサーバー側でコードを実行できる、深刻度Critical（CVSS 9.5）の脆弱性が見つかった。同日中に修正版v16.3.6・v15.5.26が緊急リリースされた。

**何が変わる**：SVGの属性値・スタイル文字列を組み立てる箇所にXMLエスケープを追加した。要素名・属性名の妥当性検証、SVGをdata URLへ埋め込む際の`&`エンコードもあわせて追加している。

**なぜ変えた**：テキストノードの中身は元から`escape_html`でエスケープされていたが、`style`・`filter`・`clip-path`のようにテンプレートリテラルで組み立てる属性値はエスケープを通っていなかった。

```js
// 修正前（@vercel/og の旧バンドルより）
style: b.filter ? `filter:${b.filter}` : void 0,
// b.filter が攻撃者制御なら、ここでSVG属性の外へエスケープできる
```

**何に効く・誰が困る**：`runtime: 'nodejs'`を指定した`ImageResponse`で、ユーザー入力をSVGの内容・属性・スタイルへそのまま渡しているアプリ。Edge実装は影響を受けない。影響バージョンはnpm `next` 16.2.0以上16.3.6未満（CVE-2026-94545）。

**ここまでの経緯**：このダイジェストは今回が初回のため、報告から修正までの経緯は追えていない。確認できた事実だけを書く。

- 2026-09-22 17:15 UTC（2026-09-23 02:15 JST）：v16.3.6公開、アドバイザリGHSA-vcvr-r3jv-pc5j公開
- 同日中：15.x系にv15.5.26としてバックポート
- 2026-09-22 22:29 UTC（2026-09-23 07:29 JST）：mainブランチ向け修正PR #99061マージ（リリース用ブランチとmainのタイムスタンプが前後しているのは、別ブランチでの作業タイミングの違いによるものと考えられる。ここは推測）

出典: https://github.com/vercel/next.js/pull/99061 、https://github.com/vercel/next.js/security/advisories/GHSA-vcvr-r3jv-pc5j

## 今日の深掘り

### next/ogのSVGシリアライズは、なぜ属性値のエスケープを見落としていたか

選んだ理由：本日最大のニュースであり、「テキストノードは注目されるのでエスケープされるが、属性値は見落とされやすい」というエスケープ漏れの典型パターンとして教材価値が高い。

背景：`next/og`はSatoriというライブラリをベースに、Reactライクな要素ツリーをSVGへ変換してPNG画像を生成する。SVG化の際、テキストノードの中身は早くから`escape_html`（`"`,`'`,`&`,`<`,`>`をエンティティに置換する小さな関数）を通していたが、`style`・`filter`・`clip-path`・`stroke`のような属性値は文字列テンプレートで直接組み立てられており、エスケープを通っていなかった。

変更点：修正コミット（PR #99061）は`@vercel/og`のバンドル済みコード自体を、パッチの当たった新しいSatori（0.29.0ベース、0.33.5まで許容）でビルドし直す形で入っている。差分を見ると、要素を組み立てる関数（`M`/`C`のような小さなヘルパー）の呼び出し側で、テキストノードだけでなく属性値の組み立てもエスケープ経由に変わっている。

```js
// 修正後（バンドル差分より）
let S2 = { ..., style: b.filter ? `filter:${b.filter}` : void 0, ... },
    T = C("text", { ...S2, fill: b.color }, (0, import_escape_html2.default)(t)) + (m2 || "");
```

周辺コード：SVGを組み立てる関数群は、Reactの`createElement`に似た形でノードツリーを受け取り、要素名・属性・子要素を文字列として連結してSVGソースを作る。エスケープをテキストノードの出力箇所だけに置くと、属性値を組み立てる別の場所までは防げない。今回の修正は、SVG文字列に値を書き込む出入り口（シリアライザの境界）を1箇所に絞り、そこで一律にエスケープする設計に寄せている。

この変更から分かる設計思想：エスケープは「値を使う場所ごと」に書くと漏れが起きやすい。出力の境界に寄せて一律に処理する方が、新しい属性が増えても安全性が保たれる。

出典: https://github.com/vercel/next.js/pull/99061 、アドバイザリ: https://github.com/vercel/next.js/security/advisories/GHSA-vcvr-r3jv-pc5j

### oxcレキサーが、曖昧トークンの判定を「3段階の状態機械」に書き換えた

選んだ理由：`/`が除算か正規表現か、`<`が比較演算子かJSXの開始かといった「前後の文脈を見ないと決められないトークン」の判定は、多くの言語処理系が抱える設計課題で、パーサー/レキサーの内部設計を学ぶ題材として適している。

背景：JavaScriptの字句解析は、1文字先読みだけでは決められないトークンを含む。`x / y`の`/`は除算だが、`function*(){ yield /re/ }`の`/`は正規表現になりうる。従来のoxcの実装は、こうした曖昧トークンをその場その場のロジックで処理していたが、キーワードの綴りをそのまま使ったプロパティ名やクラス継承式、ASI（自動セミコロン挿入）が絡むケースで誤判定するバグに加え、`yield`を含むgeneratorの中で除算が続くようなファイルでは判定コストが二次関数的に増えて遅くなる問題があった。

変更点：PR #26919は、曖昧トークンの判定を3段階の状態機械に整理した。コミットメッセージから引用する。

```
Level 1 - O(1) look behind; If it's context-independent we resolve the
ambiguity from the previous token.
Level 2 - Finite backward check, context-independent resolves some cases
that are rare in user-code
Level 3 - Full state-machine that runs essentially the JS lexical rules
to resolve the grammar; This is for fully context-dependent ambiguity
and almost never appears in real user-authored code.
```

多くのコードは直前のトークンだけで判定できるLevel 1（O(1)）で片付き、レアなケースだけがLevel 2の有限の後方チェック、さらに稀なケースだけがLevel 3のフル状態機械（JSの字句規則をなぞる）に到達する。「発生したときだけ払う（pay-when-occurs）」設計により、最悪計算量も従来の二次関数的な悪化ではなく、ファイル全体をトークン単位で1回なめるO(N)に抑えている。判定対象そのものは`crates/oxc_lexer/src/pipeline/disambiguate/operator/mod.rs`のドキュメントコメントに残っている。

```rust
//! Is a position directly after a complete value?
//!
//! [`not_operator_position`] answers this for `carve`, to tell a regex from division,
//! and a JSX element from less-than. Its doc comment explains the question in detail.
```

周辺コード：`carve`と呼ばれるモジュールが字句解析の初期段階でトークン列を粗く切り出し、この曖昧トークン判定（disambiguate）はその内部で呼ばれる。変更は`pipeline/carve/js.rs`、`pipeline/carve/jsx/`配下、`pipeline/disambiguate/`配下の広い範囲（47ファイル、5800行超）に及ぶ。

この変更から分かる設計思想：曖昧さの解決コストを「発生頻度に比例させる」設計は、ホットパス（大多数の単純なコード）の速度を犠牲にせず、レアケース（極端な入力を含む）の正しさも諦めないための定石である。コミットメッセージにある通りまず「correct」を優先し、保守性のためのリファクタリングは後続のPRに回すという段階的な進め方も読み取れる。

出典: https://github.com/oxc-project/oxc/pull/26919

## 仕様の変更

### whatwg/html

#### javascript: URLナビゲーションの後続修正が入る（CSPによるURL書き換え・iframe読み込みの安全策）

【HTML】"evaluate a javascript: URL"アルゴリズム周りの後続修正。synthetic requestにclientを追加し、CSPの`require-trusted-types-for`事前ナビゲーションチェックがリクエストURLを書き換えうる点を明記した。既定ポリシーでiframeのコンテナが取り除かれるケースに備え、loadイベント発火前にコンテナがnullでないことを確認するよう変更し、アルゴリズムにナビゲーションIDも渡すようにした。
なぜ変えたか：直前のjavascript: URLナビゲーション変更のフォローアップで、Trusted Types関連の不整合を解消するため。Chromium issue 564576916、WebKit bug 324819、wpt#62857と紐づく。
出典: https://github.com/whatwg/html/pull/12978

### w3c/csswg-drafts

#### filter-effects-1にクリックジャッキング対策の制限が明文化される

【CSS】cross-originのiframeに対するSVGフィルター適用を制限し、same-originのiframeには従来通り適用を許可する規定をFilter Effects仕様に追加した。SVGフィルターでiframeの見た目を操作・隠蔽するクリックジャッキング攻撃を防ぐのが狙いで、Chromeの機能提案を仕様に落とし込んだもの。レビューでは「制限をすべてのiframeに広げるか、cross-originだけにするか」が論点になり、「same-originフレームはセキュリティ境界ではない」という理由でcross-origin限定に落ち着いた。
なぜ変えたか：SVGフィルターを使ったクリックジャッキング攻撃を仕様レベルで塞ぐため。
出典: https://github.com/w3c/csswg-drafts/pull/13846

動きなし：whatwg/dom, whatwg/fetch, whatwg/streams, whatwg/url, w3c/aria, w3c/ServiceWorker, w3c/wcag3, tc39/proposals, tc39/ecma262

## ブラウザの実装

### Chrome Platform Status / blink-dev

取得できず（chromestatus.com、groups.google.com/a/chromium.orgはこの実行環境のegressポリシーでブロックされており、直接のページ取得ができなかった。WebSearch経由でも対象期間に絞った内容の裏取りができなかったため、今回は掲載を見送った）。

### web-platform-tests/interop

#### Interop 2027の提案受付が始まる

【Interop】ブラウザ横断のテスト同期プロジェクト「Interop」の次年度版、Interop 2027のフォーカスエリア・調査プロジェクトの提案受付がオープンし、2026-09-23（JST）に12件のIssueが一斉に立った。段階としては実装より前の、来年どの機能をInterop対象にするかを決める提案段階。Focus Area Proposal（成熟した仕様＋WPTの十分なテストがある機能が対象）とInvestigation Effort Proposal（テストを書く上での基盤課題の調査）の2種類がある。
なぜ：毎年恒例のInteropサイクルで、次年度に各ブラウザベンダーが揃って実装・テストを揃える機能セットを、コミュニティ提案から決めるため。

提案の内訳（提出のみ、採否はまだ）：

- 【Interop】CSS `sibling-index()` / `sibling-count()` 関数（Focus Area）（出典: https://github.com/web-platform-tests/interop/issues/1460）
- 【Interop】CSS `inherit()` 関数（Focus Area）（出典: https://github.com/web-platform-tests/interop/issues/1453）
- 【Interop】CSS `ident()` 関数（Focus Area）（出典: https://github.com/web-platform-tests/interop/issues/1452）
- 【Interop】CSS `element()`（Investigation Effort）（出典: https://github.com/web-platform-tests/interop/issues/1451）
- 【Interop】CSS `baseline-source` プロパティ（Focus Area）（出典: https://github.com/web-platform-tests/interop/issues/1450）
- 【Interop】アンカーポジショニング（transformとanimation対応）（Focus Area）（出典: https://github.com/web-platform-tests/interop/issues/1457）
- 【Interop】top layerのアニメーション（`display`・`content-visibility`のアニメーション可能化）（Focus Area）（出典: https://github.com/web-platform-tests/interop/issues/1456）
- 【Interop】AVIF画像フォーマットのテスト整備（Investigation Effort）：Chromium・Gecko・WebKitともAVIFを実装済みだがWPTのテストがほとんどなく、ビット深度・色空間・HDR・alpha・アニメーションなどのテスト整備を提案（出典: https://github.com/web-platform-tests/interop/issues/1461）
- 【Interop】JPEG XL/AVIFのプログレッシブレンダリング（Focus Area）（出典: https://github.com/web-platform-tests/interop/issues/1459）
- 【Interop】Web Speech API（Investigation Effort）（出典: https://github.com/web-platform-tests/interop/issues/1458）
- 【Interop】Gamepad APIの自動テスト（Virtual Gamepad Controlを使った自動化）（Investigation Effort）（出典: https://github.com/web-platform-tests/interop/issues/1454）
- 【Interop】モバイルでのビューポート挙動（Investigation Effort）（出典: https://github.com/web-platform-tests/interop/issues/1442）

このほか、Interop 2026のfetchテストから重複テストを外すテスト整理案も同日に上がっている（出典: https://github.com/web-platform-tests/interop/issues/1455）。

提案の進め方は提案ガイド参照（出典: https://github.com/web-platform-tests/interop/wiki/Interop-2027-proposal-guide）。

## OSSのPR

### React

#### Reactコンパイラのlintが、エフェクト内のawait後のsetStateを許すようになる

【React】`react-hooks/set-state-in-effect`ルールが、useEffect内でawaitした後のsetStateを「エフェクト内の同期的な連鎖レンダー」と誤検知していた。awaitの後はマイクロタスクで再開され、エフェクト本体はもう返っているので警告は誤り。パスセンシティブなデータフロー解析で、await未経由の経路だけを検知するようにした。TS版とRust版の両方に実装。
なぜ変えたか：データ取得でよく書くパターンの誤検知（Issue #34905）を解消するため。
出典: https://github.com/facebook/react/pull/36734

#### Flightが、文字列先頭のU+FEFFを落とさなくなる

【React】React Server Components（Flight）のクライアントデコーダーで、文字列先頭のU+FEFF（BOM）がTextDecoderにエンコーディングの印として消費され、中身から欠けていた。Node/Web両方のデコーダーで`ignoreBOM: true`を指定して残すようにした。
なぜ変えたか：U+FEFFで始まる文字列をシリアライズするとデータが壊れるため。
出典: https://github.com/facebook/react/pull/37625

その他：

- Rust版コンパイラで、一部のcomputedプロパティをshorthandと誤認していたのを修正（[#37674](https://github.com/facebook/react/pull/37674)）

### Next.js

#### next/ogのSVGシリアライズを堅牢化（重大セキュリティ修正）

【Next.js】→ トップニュースおよび今日の深掘り参照。
出典: https://github.com/vercel/next.js/pull/99061

#### Turbopackが、namespaceで漏れるモジュールでもエクスポート名を短縮できるようになる

【Next.js】namespace importのような「列挙できない読み取り」が一つでもあると、そのモジュールのエクスポート名のmangle（短縮）が丸ごと無効になっていた。モジュールをfacade（外向けに元の名前を保つ）とlocals（実装を短縮名で持つ）に分け、エクスポートごとの参照をモジュールグラフに載せることで、他のエクスポートは短縮を保てるようにした。クライアントで最大7.6kB（gzip）、サーバーで最大82.3kB（raw）減った。
なぜ変えたか：一つの広い使い方のせいで、他の静的な使い方までバンドル削減の恩恵を失っていたため。
出典: https://github.com/vercel/next.js/pull/97770

#### PPFで、シェルやプリフェッチをどこまで静的にするか設定で選べるようになる

【Next.js】Partial Prerendering向けの新設定`unstable_ensureStatic`。`"shell"`はランタイムデータの追跡をプリフェッチの段階まで遅らせてシェルを常に静的にし、`"prefetch"`は追跡自体を省いてシェルとプリフェッチの両方を静的にする。
なぜ変えたか：ルートのどこでランタイムデータを読んでよいかを細かく決め、全部を静的にしなくても事前レンダリングを効かせられるようにするため。
出典: https://github.com/vercel/next.js/pull/98191（関連: #98190, #98357, #98129, #99075）

#### next dev/buildが、アップグレードやセキュリティ更新を促すようになる

【Next.js】手動の`next dev`/`next build`で、Future Defaultsへの移行、最新版へのアップグレード、セキュリティ更新を促すプロンプトを出す。`experimental.agenticAutoUpgrade`を見て、人向けとAIエージェント向けで文面を分ける。既読はリポジトリやアプリ単位で覚える。
なぜ変えたか：開発者がセキュリティ修正を自分で探さなくても気づけるようにするため。
出典: https://github.com/vercel/next.js/pull/98872（関連: #98873, #98874）

その他：

- Cached Navigationsで、キャッシュ済みのページ遷移の中身が欠けることがあったのを修正（[#98767](https://github.com/vercel/next.js/pull/98767)）
- Turbopackで、namespace再エクスポートの先のメンバーの追跡が切れてHMRが壊れるのを修正（[#96396](https://github.com/vercel/next.js/pull/96396)）
- root params（`[locale]`など）をまたぐと、privateキャッシュのセグメント再利用の判定を誤るのを修正（[#98810](https://github.com/vercel/next.js/pull/98810)）
- ISRのフォールバックが確定版に変わるとき、App Shellを取り直さないように（[#94808](https://github.com/vercel/next.js/pull/94808)）
- 遷移先がリダイレクトするとprefetchがループするのを修正（[#98998](https://github.com/vercel/next.js/pull/98998)）
- `"use cache"`配下のnext/imageが、デプロイをまたぐと`NEXT_DEPLOYMENT_ID`を引き継げないのを修正（[#98739](https://github.com/vercel/next.js/pull/98739)）
- Turbopackで、動的importされる未使用の再エクスポートで出る「ModuleId not found」を修正（[#99090](https://github.com/vercel/next.js/pull/99090)）
- Turbopackで、webpackローダーを通したファイル内のimportが「Unknown module type」になるのを修正（[#99088](https://github.com/vercel/next.js/pull/99088)）
- Turbopackが、webpackローダー自体の変更を検知して再評価するように（[#95732](https://github.com/vercel/next.js/pull/95732)）
- Turbopackのファイルトレースに、パッケージが参照するプロジェクトの静的ファイルを含めるように（[#98712](https://github.com/vercel/next.js/pull/98712)）
- Turbopackのビルドのトレースを、JS側のトレースへ転送するように（[#98408](https://github.com/vercel/next.js/pull/98408)）
- hydrationエラーのツリー表示の実装を統一（[#99008](https://github.com/vercel/next.js/pull/99008)）
- エージェント向けフィードバック要求が、CIとテレメトリのオプトアウトを尊重するように（[#99062](https://github.com/vercel/next.js/pull/99062)）
- 開発オーバーレイのヘッダーが小さい画面で崩れるのを修正（[#99049](https://github.com/vercel/next.js/pull/99049)）
- turbo-tasks-backendのテストをWASM上でも動かせるように（[#97901](https://github.com/vercel/next.js/pull/97901)）
- Turbopackのファイル監視のfuzzテストを強化（[#98863](https://github.com/vercel/next.js/pull/98863)）
- ファイル監視のRustクレートnotifyを9.0.0-rc.5に更新（[#98737](https://github.com/vercel/next.js/pull/98737)）
- turbo-rcstrから16バイトポインタ幅の対応を削除（[#98355](https://github.com/vercel/next.js/pull/98355)）
- turbo-tasksの不要なアノテーションとトレイトを削除（[#98961](https://github.com/vercel/next.js/pull/98961)）
- 使われていないlernaの設定を削除（[#99039](https://github.com/vercel/next.js/pull/99039)）

### Vite

その他：

- create-viteで「TanStack Router」を選ぶとTanStack Startが生成されていたのを修正（[#23543](https://github.com/vitejs/vite/pull/23543)）
- 事前エイリアス解決の`find`を`some`に置き換え（[#23554](https://github.com/vitejs/vite/pull/23554)）

### Rolldown

#### CJSの外部依存をESM化するとき、`module.exports`を優先するようになる

【Rolldown】esm_external_requireプラグインで、外部モジュールが`module.exports = function(...)`の形だと、従来のnamespaceスプレッドでは関数として呼べなくなっていた。`module.exports`をそのまま使うよう優先順位を変えた。
なぜ変えたか：大規模モノレポで、動的にリンクされるfacadeがこの形を使っていて実際に壊れていたため。
出典: https://github.com/rolldown/rolldown/pull/10901

その他：

- strictExecutionOrderのコード分割で、再エクスポートされたnamespaceの先が初期化されないのを修正（[#10694](https://github.com/rolldown/rolldown/pull/10694)）
- WebContainerでWASIターゲットの記録が漏れてエラーになるのを修正（[#10952](https://github.com/rolldown/rolldown/pull/10952)）
- 監視対象ファイルの管理をFsWatcherに一本化（[#10949](https://github.com/rolldown/rolldown/pull/10949)）
- 自作のlintルールを組み込みの`no-restricted-properties`に置き換え（[#10933](https://github.com/rolldown/rolldown/pull/10933)）

### oxc

#### レキサーの曖昧トークン判定を状態機械に書き換え

【oxc】→ 今日の深掘り参照。
出典: https://github.com/oxc-project/oxc/pull/26919（関連: #26931, #26939, #26940, #26947, #26948, #26949, #26950）

#### napiパッケージに、スレッドを使わないWASIビルドが加わる

【oxc】parser・minify・transformなどのnapiパッケージに、`wasm32-wasip1`のスレッドなしビルドを追加した。ブラウザやCloudflare Workersのように、マルチスレッドが使えない環境向け。
なぜ変えたか：スレッドが使えない環境でもoxcを動かせるようにし、エッジ環境への対応を広げるため。
出典: https://github.com/oxc-project/oxc/pull/26898

#### パーサーが、孤立サロゲートを含むエクスポート名を拒否するようになる

【oxc】`export { x as "\uD800" }`のような、不正なUnicode（孤立サロゲート）を含むエクスポート名を、エラーを報告しつつ回復していたのをやめ、回復しない致命的なエラーにした。
なぜ変えたか：仕様上Early Errorであるべきもので、Acorn・Babelも同じく拒否しているため。
出典: https://github.com/oxc-project/oxc/pull/26953

その他：

- `unified-signatures`ルールをtypescript-eslintの最新の挙動に合わせた（[#26956](https://github.com/oxc-project/oxc/pull/26956)）
- `import/no-duplicates`が、import属性の違うimportを重複扱いしないように（[#26936](https://github.com/oxc-project/oxc/pull/26936)）
- `node/no-exports-assign`の分類をstyleからsuspiciousへ変更（[#26555](https://github.com/oxc-project/oxc/pull/26555)）

動きなし：reactjs/rfcs, oxc-project/oxc Discussions（議論ウォッチ参照）, vercel/next.js Discussions

### Node.js

#### Web StreamsのpipeTo/teeが、内部のオブジェクト生成をやめて速くなる

【Node.js】pipeTo・tee・byte teeが毎回作っていた、computed-symbolキー付きのオブジェクトリテラルを、使い回せる小さなクラスのインスタンスに置き換えた。pull()のないソースでの余計なマイクロタスクや、`ReadableStreamBYOBReader.prototype.read()`のasync関数ラップも削り、仕様どおり直接Promiseを返すようにした。
なぜ変えたか：プロファイルで1回あたり約1マイクロ秒の無駄が見つかったため。仕様の挙動に近づけながら速くしている。
出典: https://github.com/nodejs/node/pull/66154

#### Nodeを組み込むアプリが、自前のWasmストリーミング処理を残せるようになる

【Node.js】`SetIsolateMiscHandlers()`は、`WebAssembly.compileStreaming()`の処理を必ずNode.js自前の実装で上書きしていた。独自のネットワークスタックを持つ組み込み先（Electronなど）向けに、上書きしないための`SHOULD_NOT_SET_WASM_STREAMING_CALLBACK`フラグを追加した。
なぜ変えたか：組み込み先が毎回コールバックを設定し直す回避策を不要にするため。
出典: https://github.com/nodejs/node/pull/65690

その他：

- cryptoが、実行中のFIPSモードの切り替えをその場で反映するように（[#66160](https://github.com/nodejs/node/pull/66160)）
- vfsのZipProviderの`readdir`が`recursive`に対応（[#66127](https://github.com/nodejs/node/pull/66127)）
- vfsのZipProviderが、ディレクトリでない親の下へのエントリ作成を拒否するように（[#65829](https://github.com/nodejs/node/pull/65829)）

### Bun

#### node:fsのコールバックを、Promiseを経由せずネイティブの完了から直接呼ぶようになる

【Bun】fsのコールバックAPIは、内部でPromiseを作ってからコールバックを呼んでいた。これをネイティブ側の完了通知から直接JSのコールバックを呼ぶ形にした。
なぜ変えたか：Promiseを挟むことで生じるマイクロタスクの余分な1手と、trace eventsの不正確さをなくすため。
出典: https://github.com/oven-sh/bun/pull/43728

#### TLSで、ハンドシェイク前に書いたデータを最後のハンドシェイクと同じセグメントで送るようになる

【Bun】ハンドシェイクが終わる前にアプリが書いたデータを、ハンドシェイクの最後の送信と同じTCPセグメントにまとめて送る。
なぜ変えたか：別のセグメントに分かれることで生じていた、余計な往復と遅延を減らすため。
出典: https://github.com/oven-sh/bun/pull/43766

その他：

- プロキシトンネル・Duplex・名前付きパイプでも、クライアント証明書を送る前に不正な証明書チェーンを拒否するように（[#43767](https://github.com/oven-sh/bun/pull/43767)）
- 子プロセスに渡すIPCのfdをブロッキングにし、dup2の元がstdioと重ならないように（[#43814](https://github.com/oven-sh/bun/pull/43814)）
- S3のマルチパートアップロードが永久にpendingになるケースを中断するように（[#41688](https://github.com/oven-sh/bun/pull/41688)）
- `node:vm`の`importModuleDynamically`が早く解放されてクラッシュするのを修正（[#43724](https://github.com/oven-sh/bun/pull/43724)）
- バイトコードを使わないチャンクで、余分な出力ファイルを作らないように（[#43822](https://github.com/oven-sh/bun/pull/43822)）
- `buffer.transcode()`が2GiB以上の出力で異常終了するのを修正（[#42875](https://github.com/oven-sh/bun/pull/42875)）
- 非同期イテラブルなbodyの`ERR_INVALID_THIS`が握りつぶされるのを修正（[#43758](https://github.com/oven-sh/bun/pull/43758)）
- FileSinkのパイプの後始末を専用のfinalizeに分離（[#43729](https://github.com/oven-sh/bun/pull/43729)）
- `child_process`のstdioに渡した`tls.TLSSocket`を、Nodeと同じく拒否するように（[#43722](https://github.com/oven-sh/bun/pull/43722)）
- Bun.Imageが、警告付きでデコードできたJPEGを扱えるように（[#43812](https://github.com/oven-sh/bun/pull/43812)）
- orderfileの生成で、実アプリのワークロードをトレースできるように（[#43784](https://github.com/oven-sh/bun/pull/43784)）
- orderfileの生成で、無視するシグナルをプロセス自身に問い合わせるように（[#43820](https://github.com/oven-sh/bun/pull/43820)）
- 11クレートの読まれないフィールドと使われないオプションを削除（[#43745](https://github.com/oven-sh/bun/pull/43745)）

### TypeScript

#### tsc -bが、依存の浅いプロジェクトから順に投入して約2倍速くなる

【TypeScript】プロジェクト参照のビルド順を、深さ優先の走査から、依存の浅いプロジェクトを先に投入する安定ソートに変えた。19,430プロジェクトのモノレポでは、32のビルダーのうち平均13しか働いていなかったのが、ビルド時間の中央値で530秒から262秒になった（ピークメモリは約15%増）。
なぜ変えたか：依存の深いプロジェクトを先に拾うと、他のビルダーが上流の完了待ちで遊んでしまっていたため。
出典: https://github.com/microsoft/TypeScript/pull/64220

#### オブジェクトの分割代入のデフォルト値の扱いが、配列と揃う

【TypeScript】オブジェクトの分割代入パターンは、初期化子を持つ要素だけを「欠けている要素」として補っていたが、配列パターンは欠けている要素をすべて補っていた。この食い違いをなくした。
なぜ変えたか：同じ意味のオブジェクト引数と配列引数で、型推論や診断の結果が違っていたため。
出典: https://github.com/microsoft/TypeScript/pull/64043

その他：

- tsgoのファイル監視で、macOSのFSEventsが返す大文字小文字の違いでイベントが落ちるのを修正（[#64210](https://github.com/microsoft/TypeScript/pull/64210)）
- オブジェクトのプロパティ名の補完が、クォートの設定に従うように（[#64359](https://github.com/microsoft/TypeScript/pull/64359)）
- tsgoのAPIで、tsconfigの`plugins`の解析とMappedTypeのプロパティの取得に対応（[#64397](https://github.com/microsoft/TypeScript/pull/64397)）
- tsgoのAPIで、ファイル名の大文字小文字に関する不具合を2つ修正（[#64391](https://github.com/microsoft/TypeScript/pull/64391)）
- 診断メッセージの巨大なマップを遅延初期化にして、Goのビルドを速く（[#64402](https://github.com/microsoft/TypeScript/pull/64402)）
- tsgoの自作lintで、`break`だけのcaseを禁止（[#64153](https://github.com/microsoft/TypeScript/pull/64153)）
- コード生成のキャッシュをリポジトリルートごとに分離（[#64395](https://github.com/microsoft/TypeScript/pull/64395)）
- ローカライズとベンダーコードの生成もキャッシュ対象に（[#64392](https://github.com/microsoft/TypeScript/pull/64392)）
- ベンダーコードの最新性チェックを簡略化（[#64393](https://github.com/microsoft/TypeScript/pull/64393)）
- enumの生成処理をtools/scriptsへ移動（[#64381](https://github.com/microsoft/TypeScript/pull/64381)）
- VS Code向けローカライズツールのバージョンを固定（[#64403](https://github.com/microsoft/TypeScript/pull/64403)）

### Biome

#### CSSの物理方向プロパティを論理プロパティに置き換えるよう促すlintルールが入る

【Biome】新ルール`useLogicalProperties`。`left`/`right`のような物理方向のプロパティを、`inset-inline-start`のような論理プロパティへ置き換えるよう促す。
なぜ変えたか：右から左に書く言語や縦書きのレイアウトでも崩れにくいCSSにするため。
出典: https://github.com/biomejs/biome/pull/10816

#### 型推論エンジンが、組み込みの型と型演算子を広くカバーし始める

【Biome】独自の型推論エンジンのコード生成に、グローバル関数の宣言、`Date`の全メンバー、`Intl`、計算メンバーやシンボルキーのアクセス、indexed access型、`keyof`の処理を順に追加した。
なぜ変えたか：型を使うlintルールが、組み込みAPIのシグネチャや型演算子を正しく解決できるようにするため。
出典: https://github.com/biomejs/biome/pull/11894（関連: #11892, #11891, #11890, #11889, #11886, #11885）

その他：

- 設定ファイルにエラーがあるとき、黙って続けず明示的にエラー終了するように（[#11910](https://github.com/biomejs/biome/pull/11910)）
- HTMLパーサーで、`>`から始まる正規表現を正しく字句解析するように（[#11907](https://github.com/biomejs/biome/pull/11907)）
- 閉じ`>`がないとき、`<<`を型引数と誤解析しないように（[#11908](https://github.com/biomejs/biome/pull/11908)）
- `useForOf`が、本体でインデックスを書き換えるループを誤検知しないように（[#11906](https://github.com/biomejs/biome/pull/11906)）

### Solid

main（1.x系）：動きなし。直近コミットは2026-09-04で、対象期間内のマージなし。

next（2.0系、以下すべて`next`ブランチへのマージ）：

#### cleanupの実行順が、1.xと同じ「巻き戻し順」に戻る

【Solid】2.0で崩れていた1.x由来のLIFO（後入れ先出し）のルールを戻し、`onCleanup`は登録の逆順、子は親より先にcleanupされるよう統一した。
なぜ変えたか：本番ビルドと開発ビルドで、親と子のどちらが先にcleanupされるかが違っていたため。
出典: https://github.com/solidjs/solid/pull/3591

#### 非同期のcomputationでawait後に読み取ると、開発時に警告が出るようになる

【Solid】async computationの中でawaitより後に読んだ値は追跡されないため、依存が変わっても再計算されない。開発ビルドで、V8の非同期スタックトレースを使ってこれを検出し、警告する。
なぜ変えたか：「awaitの後の読み取りは追跡されない」という非同期リアクティビティの罠に、開発中に気づけるようにするため。
出典: https://github.com/solidjs/solid/pull/3602

#### シグナルやエフェクトの実行を、Chrome DevToolsのPerformanceパネルで見られるようになる

【Solid】新パッケージ`@solidjs/web/performance-tracks`。シグナルとエフェクトの実行を、Performanceパネルのカスタムトラックとして表示する。
なぜ変えたか：どの更新がどこで起き、何が再計算されたかを、ブラウザ標準の開発者ツールで追えるようにするため。
出典: https://github.com/solidjs/solid/pull/3580

その他：

- SSRのspread要素で静的テキストがエスケープされず、HTMLインジェクションになりえたのを修正（[#3559](https://github.com/solidjs/solid/pull/3559)）
- 楽観的更新で溜めた未確定の値（held truth）を隠す範囲を、laneの経路だけに限定（[#3590](https://github.com/solidjs/solid/pull/3590)）
- projectionのdraftが書き込めるかの判定を、実行ごとのトークンと破棄フラグで行うように（[#3595](https://github.com/solidjs/solid/pull/3595)）
- 保留中のフレームが、owner破棄時にcleanupされず復活するのを修正（[#3592](https://github.com/solidjs/solid/pull/3592)）
- 自身のrootをdisposeするcleanupが二重に実行されるのを修正（[#3606](https://github.com/solidjs/solid/pull/3606)）
- ownerのないoverrideの書き換えが、新しいトランザクションを開いてしまうのを修正（[#3586](https://github.com/solidjs/solid/pull/3586)）
- 休眠から再起動したmemoが、ownerのチェーンに再接続されないのを修正（[#3555](https://github.com/solidjs/solid/pull/3555)）
- `LOADING_ON_OUTSIDE_HOLD`の警告を、確実な違反のときだけ出すように（[#3584](https://github.com/solidjs/solid/pull/3584)）
- hybridなmemo・signal・storeのハイドレーションが、サーバー応答の到着を待つように（[#3605](https://github.com/solidjs/solid/pull/3605)）
- SSRのレンダリング後に起きた非同期の拒否でプロセスが落ちないように（[#3594](https://github.com/solidjs/solid/pull/3594)）
- useHeadで読むスタイルシートがハイドレーションを止めるのを修正（[#3600](https://github.com/solidjs/solid/pull/3600)）
- children以外のpropで渡したJSXで、ハイドレーションキーがずれるのを修正（[#3599](https://github.com/solidjs/solid/pull/3599)）
- SSRで、spreadの後ろの属性を静的なmarkupとして出力するように（[#3562](https://github.com/solidjs/solid/pull/3562)）

### React Hook Form

#### フォームレベルのvalidateが、常に最新の関数で実行されるようになる

【RHF】`useForm`のフォームレベルの`validate`が、再レンダーをまたいで古い関数のまま実行されることがあった。常に最新の関数を使い、設定から外されたvalidateは実行しないようにした。
なぜ変えたか：新しい関数を渡しても古い関数が使われ続けるのは、Reactの再レンダーのモデルと合わないため。
出典: https://github.com/react-hook-form/react-hook-form/pull/13777

その他：

- useFieldArrayで、touchedFieldsの更新が本当に必要なときだけform stateに含めるように（[#13776](https://github.com/react-hook-form/react-hook-form/pull/13776)）

（この2件は`useFieldArray.ts`とフォームレベルvalidateの解決処理が対象で、本の「フォームの値はどこに置かれているか」「formStateのどのキーを読んだかを覚える」章が引用する箇所とは重ならないため、本への影響はなし）

### Ladybird（masterブランチ）

#### Compositorが、LibWebから切り出した共有部分だけを使うようになる

【Ladybird】DOM・CSS・JSを扱わないCompositorプロセスが、LibWeb全体をリンクしていた。display list、スクロール状態、入力イベントの型、WebGLのコマンド列など、CompositorとWebContentで共有する部分だけを新しい`LibCompositing`に切り出した。続く#12153でLibWebViewへの依存も外し、読み込むライブラリは29個から15個に減った。
なぜ変えたか：プロセスごとに要らないライブラリを読み込まず、メモリを減らすため。
出典: https://github.com/LadybirdBrowser/ladybird/pull/12152（関連: #12153）

#### 同じ内容の`<style>`要素で、コンパイル済みのスタイルシートを共有するようになる

【Ladybird】解析済みの内容とbase URLをキーに、コンパイル済みのスタイルシートを共有する。CSSOMで書き換えられたときは、コピーオンライトで自分専用の複製を作る。wpt.fiのトップページで、メモリ使用量が2.41GBから1.55GBに減った。
なぜ変えたか：同じ内容の`<style>`が大量にあるページで、パースと保持のコストが重複していたため。
出典: https://github.com/LadybirdBrowser/ladybird/pull/12143

#### RegExpの高速パスが、ユーザーが差し替えたフックを飛ばさなくなる

【Ladybird】`exec`や`flags`のgetterを上書きしたカスタムRegExpが、`replace`・`split`・`test`の高速パスで無視されていた。V8・JSC・SpiderMonkeyと同じく、スロットとポインタの比較だけで「観測されない」と確かめられるときにだけ高速パスに入るようにした。
なぜ変えたか：仕様上は観測できるはずのフックが無視されると、正規表現のポリフィルやモンキーパッチが壊れるため。
出典: https://github.com/LadybirdBrowser/ladybird/pull/12142

その他：

- スタイル計算とレイアウトの一時的なヒープ確保を削減し、StyleBenchが+1.73%（[#12154](https://github.com/LadybirdBrowser/ladybird/pull/12154)）
- spreadの大きいinsetのbox-shadowで、影に隙間ができるのを修正（[#12150](https://github.com/LadybirdBrowser/ladybird/pull/12150)）
- 終了時にstoreがDB接続より後に破棄されてクラッシュするのを修正（[#12128](https://github.com/LadybirdBrowser/ladybird/pull/12128)）
- GUIと重複するコンテンツブロッカーの詳細設定を削除（[#12151](https://github.com/LadybirdBrowser/ladybird/pull/12151)）
- Qt版のハンバーガーメニューにショートカットを表示（[#12148](https://github.com/LadybirdBrowser/ladybird/pull/12148)）

## 議論ウォッチ

### CSSの`flow-start`/`flow-end`整列キーワードに、Mozilla・WebKitへのポジション照会が同時に立つ

【CSS】
- 論点：Flexbox/Grid masonryで`fill-reverse`/`track-reverse`により要素の並びが反転したとき、`align-items`/`justify-items`などの`start`/`end`を「見た目上の配置」基準にするか「論理的なflow方向」基準にするかがcsswg-drafts#14052で未解決のまま、それを解決する新キーワード`flow-start`/`flow-end`をCSS Alignに追加する案が浮上している。
- 賛否：反転後も`start`/`end`の意味を変えない立場（`align-content: start`が反転後に下端を指すのは分かりにくい）と、`flex-direction: column-reverse`＋`justify-content: flex-end`が論理方向に追従する既存挙動に合わせるべきという立場が対立している。
- 今回の進展：9月22日、MozillaとWebKitの両standards-positionsリポジトリに、この提案へのポジション照会issueがほぼ同時に立った（Mozilla #1455、WebKit #723）。執筆時点ではどちらも"Unscreened"でベンダーの正式な立場はまだ示されていない。Chromiumはすでに実装に着手している。
- 次に決まりそうなこと：MozillaとWebKitがポジションを表明し、それを受けてcsswg-drafts#14052でのCSSWGの議論が進む見通し。

出典: https://github.com/mozilla/standards-positions/issues/1455 、https://github.com/WebKit/standards-positions/issues/723 、https://github.com/w3c/csswg-drafts/issues/14052

### TC39 2026年9月会議に、無効な入力文字列のエラー種別（RangeError/TypeError）統一が議題として追加される

【TC39】
- 論点：JS標準ライブラリの各APIが無効な文字列入力を受けたときに投げる例外が`RangeError`だったり`TypeError`だったりまちまちで、どちらを使うべきかの規約がこれまで明文化されていなかった。
- 賛否：議題自体はgibson042が提出。レビューでmichaelficarraから「新しい規範的な規約の話か、個別の具体的な提案の話か」を問われ、提出者は「新しい規範的な規約を確立する」ものだとタイトルを修正した。実際の賛否は会議本番でこれから交わされる。
- 今回の進展：9月22日、2026年9月TC39総会のアジェンダにこの議題を追加するPRがマージされた。
- 次に決まりそうなこと：会議本番での議論を経て、合意できれば「規範的な慣習」としてTC39内のガイドラインに反映される見通し。議事結果はまだ公開されていない。

出典: https://github.com/tc39/agendas/pull/2163

動きなし：WICG/proposals（新規issueはあったがコメント0件で論点は未着手）, tc39/proposal-signals, reactjs/rfcs, vercel/next.js Discussions（RFCカテゴリ）, oxc-project/oxc Discussions, nodejs/TSC

## 脆弱性

### next/ogのImageResponse（Node実装）にSVGインジェクションによるRCE（GHSA-vcvr-r3jv-pc5j, CVE-2026-94545）

【脆弱性】

**影響するパッケージとバージョン、深刻度**：npm `next` 16.2.0以上16.3.6未満。CVSS v4スコア9.5（Critical）。修正版は16.3.6（15.x系はv15.5.26にバックポート）。

**攻撃原理**：next/ogの`ImageResponse`（Node実装）は、渡された要素ツリーをSatoriベースのシリアライザでSVG文字列に変換してPNGを生成する。テキストノードの中身は`escape_html`でエスケープされていたが、`style`・`filter`・`clip-path`などテンプレートリテラルで組み立てる属性値はエスケープされていなかった。クエリパラメータなど攻撃者が制御できる値をこれらの属性経由でSVGに渡すと、属性値の外へ抜け出して任意のSVG要素を注入できる。

**影響バージョンと成立条件**：Node実装（`runtime: 'nodejs'`）の`ImageResponse`に、サニタイズしていないユーザー入力をSVGの内容・属性・スタイルとして渡しているアプリが対象。Edge実装は影響を受けない。

**修正コミットのdiff解説**：修正PR #99061は、`@vercel/og`をパッチの当たった新しいSatori（0.29.0ベース、0.33.5まで許容）でビルドし直す形で入っている。シリアライズの出入り口（`M`/`C`のような要素組み立て関数）で、テキストノードだけでなく属性値の組み立ても`escape_html`経由に統一し、あわせて要素名・属性名の妥当性検証、SVGをdata URLへ埋め込む際の`&`エンコードも追加した。

**同種の脆弱性パターン**：テンプレートリテラルで生成した文字列を、構造化データ（HTML/SVG/XMLなど）の一部としてそのまま埋め込むと起きるインジェクションの一種。「テキストノードだけエスケープして属性値を見落とす」のは典型的な抜け漏れパターンで、HTMLのテンプレートエンジンやSSRのレンダラーでも繰り返し起きる。

出典: https://github.com/vercel/next.js/security/advisories/GHSA-vcvr-r3jv-pc5j 、修正PR: https://github.com/vercel/next.js/pull/99061

## 仕様を読む会の候補

- **CSS Filter Effectsのクリックジャッキング対策**（w3c/csswg-drafts #13846）：SVGフィルターでiframeを覆い隠す攻撃を、仕様側の1文でどう塞いだかを読める。1件のPRで完結し、セキュリティと仕様設計の関係を追う題材として手頃（出典: https://github.com/w3c/csswg-drafts/pull/13846）。
- **oxcレキサーの曖昧トークン判定状態機械**（oxc-project/oxc #26919）：47ファイル・5800行超の変更で、今日の深掘りだけでは追いきれない設計判断（Level 2/3の具体的な実装、既存バグの再現ケースなど）が多く残っている。パーサー/レキサーパートの執筆候補にも直結する（出典: https://github.com/oxc-project/oxc/pull/26919）。
