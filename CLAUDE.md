# frontend-oss-reading-book

フロントエンドの基盤となるOSSの内部実装を、ソースコードとgit履歴を根拠に解説する本のモノレポ。mdBookでビルドし、GitHub Pagesで公開している。

## 作業の進め方

- 何を書くかは `BACKLOG.md` で管理する。ルールはBACKLOG.mdの冒頭に従う。
- 1章ごとにブランチを切り、PRを1本出す。mainに直接pushしない。
- 既存の本のディレクトリ構成、`SUMMARY.md`、文体に合わせる。新しい章は `SUMMARY.md` にも追加する。
- 書き終えたら `mdbook build` が通ることを確認してからPRを出す。

## 解説対象のOSSの読み方

- 解説対象のリポジトリは、作業ディレクトリの外（例：`/tmp/oss/<name>`）にcloneする。このリポジトリの中にcloneしたりコミットしたりしない。
- 基本は `git clone --depth 1` の浅いcloneにする。git履歴が必要なときだけ、対象のパスに絞って履歴を取得する（例：`git fetch --deepen` や `git log -- <path>`）。
- 解説は必ずソースを根拠にする。参照したファイルパス、関数名、コミットハッシュを本文に残し、どのバージョン（タグかコミット）を読んだかを章の冒頭に書く。
- 推測で書いた部分は推測だと明記する。

## 書き方

- 読者はマネージャー職で、Web仕様やOSSの内部実装を理解したい人。前提知識を飛ばさず、「なぜこういう設計なのか」を重視する。
- スマホで読む前提なので、1段落は短めにし、長いコードの引用は要点に絞る。
- 図が必要なところはMermaidで書く。

## 判断に迷ったら

- 章の分け方、対象バージョン、解説の深さなどで迷ったら、作業は止めずに自分の判断で進め、判断した内容をPRの説明に書く。
